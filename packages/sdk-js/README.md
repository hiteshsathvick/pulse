# @pulse/sdk-js (reserved)

The TypeScript ingestion SDK (`identify`/`track`/`page`, local buffering, retry with backoff)
lands in Phase 10 (`SPEC.md` §7). This directory is reserved so the monorepo layout is settled
from Phase 0; no code lives here yet.
